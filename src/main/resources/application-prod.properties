# --- Puerto: 8172 (Semilla NN=72) ---
server.port=8172

# --- ORM: JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Opcion B: PostgreSQL Local ---
spring.datasource.url=jdbc:postgresql://localhost:5432/agrosmart_db
spring.datasource.username=postgres
spring.datasource.password=Naniko.al56

spring.docker.compose.enabled=false

# --- M\u00F3dulo de IA (LangChain4j) ---
langchain4j.open-ai.chat-model.api-key=demo_error_invalido
langchain4j.open-ai.chat-model.model-name=gpt-4o-mini
langchain4j.open-ai.chat-model.timeout=30s
langchain4j.open-ai.chat-model.log-requests=true
langchain4j.open-ai.chat-model.log-responses=true
logging.level.dev.langchain4j=DEBUG